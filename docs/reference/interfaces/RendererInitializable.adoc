= RendererInitializable Reference

Extension of `Initializable` specifically for rendering components.

== Methods

=== `setup(metrics: RendererMetrics): void`
Invoked with live geometric metrics for the current session.

== General Usage

Implemented by components like `DisplayRenderer` to receive pre-calculated X/Y origins and cell sizes before the first frame is drawn.
